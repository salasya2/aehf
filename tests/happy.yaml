name: happy
eval:
  - id: "1"
    task_prompt: "What is 2*3?"
    tools:
      - name: calculator
        description: "Evaluates arithmetic"
        parameters:
          type: object
          properties:
            expression: {type: string}
    success_criteria:
      answer_regex: "611"
      required_tools: [calculator]
      rubric: "The final answer states 611."
    max_steps: 10
    timeout_seconds: 300
    token_budget: 10000
    tool_fixtures:
      calculator : "6"
    